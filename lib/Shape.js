class Shape {
    constructor(color) {
      this.color = color;
    }
  
    setColor(color) {
      this.color = color;
    }
  }
  
  module.exports = Shape;

  //Set up the Shape class as a parent class for the Triangle, Circle, and Square classes.
  